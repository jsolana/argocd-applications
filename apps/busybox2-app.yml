---
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: busybox2-echo
  namespace: argocd
spec:
  destination:
    namespace: busybox2-echo
    server: https://kubernetes.default.svc
  project: default
  source:
    path: manifests/busybox
    repoURL: https://github.com/jsolana/argocd-applications
    targetRevision: HEAD
  syncPolicy:
    automated:
      allowEmpty: true
      prune: true
    retry:
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 1m
      limit: 2
    syncOptions:
    - ServerSideApply=true